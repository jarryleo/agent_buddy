# agent_buddy

Agent Buddy

目标:跨端智能体基座

### UI布局:
1.无论哪端,都采用手机样式.
2.APP启动后进入聊天页面.左上角设置按钮,点击进入设置页面.
3.设置页面,顶部tab:Provider,Role,Tools,Skill
Provider:模型提供商列表,可以新增,编辑,删除,启用.功能见下面功能详情.
Role:AI角色扮演列表,展示角色列表,可新增,编辑,删除,启用.
Tools:AI智能体工具列表,管理内置工具,先实现Fetch Web,可以让AI获取网址内容.
Skill:技能列表,与现有市面上技能兼容.
~~ UI样式采用IOS设计样式,简约素雅风格 ~~

### 功能:
#### 模型提供商:
1.先实现设置AI模型提供商页面,需要支持OpenAI和Anthropic协议.
2.可以添加多个提供商,添加页面输入名称, url, API key,需要根据协议自动补全path,有获取模型列表按钮.有测试连接按钮.

#### 聊天首页:
1.实现对话聊天页面,支持流式输出,支持不同模型的思考模式,AI聊天气泡5行思考流式展示,回复内容后,思考部分可展开至10行,上下滚动.
2.底部输入对话框最多展示10行,超过10行后可滚动.右边发送按钮与输入框底部对齐,整个输入框部分采用悬浮条设计.

#### 设置页面:
见上面UI布局.


### 代办事项
#### 已完成
- 聊天气泡支持markdown语法
- 硬编码文字国际化处理
- 思考气泡的思考过程,需要自动滚动到底部
- 桌面端启动窗口默认尺寸按手机尺寸展示
- AI调用工具失败,需要完善,气泡内须反应AI调用工具过程以及状态
- 支持图片发送
- 新增工具,AI获取当前日期和时间
- 新增工具,询问用户工具:询问用户问题,并提供选项给用户选择,选项可以是按钮或多选框
- 桌面端新增tool:AI命令行工具.可以让AI执行命令行,同步到设置内的tools,默认关闭
- 桌面端新增tool:获取本机环境信息,系统型号版本之类的,方便AI做命令行决策,同步到设置内的tools
- 本地模型支持,设置页新增"Local"tab,可以加载gguf格式的模型文件,自动检测mmproj多模态投影,设置上下文/温度/GPU层数/最大生成token等参数(基于 llamadart)
- 每轮消息对话都要携带聊天历史消息吗？能否优化 Chat Session，查看llamadart依赖的文档： https://llamadart.leehack.com/docs/getting-started/first-chat-session ，看看有没有优化方案。同时优化聊天记录存储方案，能否根据session 存储到flutter数据库（多端支持的数据库），同时右上角的清空对话按钮改为会话管理，点击弹出会话列表，顶部为新会话按钮，下面为历史会话，每条会话显示会话时间，点击切换到对应会话，可以单条删除也可以批量删除。
- 方案探讨:手机端增加读取系统日历，读取代办，读取备忘录，读取提醒事项等功能，用于AI和用户交互，同时可以写入日历，代办，备忘录，提醒实现等功能，实现用户AI助手的功能。完善用户权限申请，可能要各机型适配，先实现 Android 和 IOS 默认支持的功能，每个功能都作为独立工具
- AI记忆系统，在设置页增加记忆tab，展示记忆列表，记忆用数据库按时间顺序排列，用户可以手动删除或增加记忆，可以批量删除，给AI增加关键词模糊查询记忆功能，
- AI记忆产生的逻辑，让AI自行判断。AI决定有用需要记住的事可以写入记忆列表，AI觉得需要查询记忆的时候自主模糊查询。
- 新增工具，AI可以获取用户所在位置，便于查询天气等，移动端可以通过定位，桌面端可以通过其它办法，例如IP地址什么的，你来设置这个工具
- 记忆系统优化，工具提示词，让AI写入记忆时多列举关键词，便于记忆模糊查找，记忆查找时，让AI可以多个关键词同时模糊查询


#### 未完成
- 桌面端新增tool:文件工具:新建文件,读取文件内容,读取文件属性,编辑文件内容,删除文件,重命名文件等.同步到设置内的tools
- 增加代办任务和定时任务,
- 新增工tools:谷歌表格API
- 新增频道，对接第三方聊天软件
- 本地模型推荐几个给用户下载

