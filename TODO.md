# agent_buddy

Agent Buddy

目标:跨端智能体基座

### UI布局:
1.无论哪端,都采用手机样式.
2.APP启动后进入聊天页面.左上角设置按钮,点击进入设置页面.
3.设置页面,顶部tab:Provider,Role,Tools,Skill
Provider:模型提供商列表,可以新增,编辑,删除,启用.功能见下面功能详情.
Role:AI角色扮演列表,展示角色列表,可新增,编辑,删除,启用.
Tools:AI智能体工具列表,管理内置工具,先实现Fetch Web,可以让AI获取网址内容.
Skill:技能列表,与现有市面上技能兼容.
~~ UI样式采用IOS设计样式,简约素雅风格 ~~

### 功能:
#### 模型提供商:
1.先实现设置AI模型提供商页面,需要支持OpenAI和Anthropic协议.
2.可以添加多个提供商,添加页面输入名称, url, API key,需要根据协议自动补全path,有获取模型列表按钮.有测试连接按钮.

#### 聊天首页:
1.实现对话聊天页面,支持流式输出,支持不同模型的思考模式,AI聊天气泡5行思考流式展示,回复内容后,思考部分可展开至10行,上下滚动.
2.底部输入对话框最多展示10行,超过10行后可滚动.右边发送按钮与输入框底部对齐,整个输入框部分采用悬浮条设计.

#### 设置页面:
见上面UI布局.


### 代办事项
#### 已完成
- 聊天气泡支持markdown语法
- 硬编码文字国际化处理
- 思考气泡的思考过程,需要自动滚动到底部
- 桌面端启动窗口默认尺寸按手机尺寸展示
- AI调用工具失败,需要完善,气泡内须反应AI调用工具过程以及状态
- 支持图片发送
- 新增工具,AI获取当前日期和时间
- 新增工具,询问用户工具:询问用户问题,并提供选项给用户选择,选项可以是按钮或多选框
- 桌面端新增tool:AI命令行工具.可以让AI执行命令行,同步到设置内的tools,默认关闭
- 桌面端新增tool:获取本机环境信息,系统型号版本之类的,方便AI做命令行决策,同步到设置内的tools
- 本地模型支持,设置页新增"Local"tab,可以加载gguf格式的模型文件,自动检测mmproj多模态投影,设置上下文/温度/GPU层数/最大生成token等参数(基于 llamadart)
- 每轮消息对话都要携带聊天历史消息吗？能否优化 Chat Session，查看llamadart依赖的文档： https://llamadart.leehack.com/docs/getting-started/first-chat-session ，看看有没有优化方案。同时优化聊天记录存储方案，能否根据session 存储到flutter数据库（多端支持的数据库），同时右上角的清空对话按钮改为会话管理，点击弹出会话列表，顶部为新会话按钮，下面为历史会话，每条会话显示会话时间，点击切换到对应会话，可以单条删除也可以批量删除。
- 方案探讨:手机端增加读取系统日历，读取代办，读取备忘录，读取提醒事项等功能，用于AI和用户交互，同时可以写入日历，代办，备忘录，提醒实现等功能，实现用户AI助手的功能。完善用户权限申请，可能要各机型适配，先实现 Android 和 IOS 默认支持的功能，每个功能都作为独立工具
- AI记忆系统，在设置页增加记忆tab，展示记忆列表，记忆用数据库按时间顺序排列，用户可以手动删除或增加记忆，可以批量删除，给AI增加关键词模糊查询记忆功能，
- AI记忆产生的逻辑，让AI自行判断。AI决定有用需要记住的事可以写入记忆列表，AI觉得需要查询记忆的时候自主模糊查询。
- 新增工具，AI可以获取用户所在位置，便于查询天气等，移动端可以通过定位，桌面端可以通过其它办法，例如IP地址什么的，你来设置这个工具
- 记忆系统优化，工具提示词，让AI写入记忆时多列举关键词，便于记忆模糊查找，记忆查找时，让AI可以多个关键词同时模糊查询
- 工具气泡没有适配主题色。桌面端实现回车键发送消息，ctrl+回出或者alt+回车换行
- fetch_web 工具优化：返回给AI的文本去掉html相关代码，只返回内容纯文本，压缩token。
- fetch_web 多级跳转：默认不返回链接列表，AI 用 link_text 参数把链接文字传进去即可拿到对应 URL，再用该 URL 调一次 fetch_web 即可进入下一级页面，重复调用同一 URL 走内存缓存。
- download 工具：AI 调用 download 工具把 URL 文件下载到 APP 临时目录。气泡内显示下载进度、状态、文件名；下载完成后出现"保存"按钮，点击弹出系统目录选择器，用户选择目录后复制文件并删除临时文件。Web 端不可用（path_provider 在 Web 上没有临时目录）。
- 设置页-工具选项顶部加一个总开关:使用工具 ON/OFF .当关闭时,所有工具不再使用.纯聊天场景节省token
- 技能的提示词注入优化:系统提示只注入技能名称+简介,新增 load_skill 工具让AI按需加载完整技能内容。详见 `LoadSkillTool` + `ChatProvider._buildSystemPrompts`
- 项目重构:请把每个工具的实现过程放入对应的工具类里面,如果能抽取公共参数或方法请抽取到工具基类.
- 桌面端新增tool:文件工具:新建文件,读取文件内容,读取文件属性,编辑文件内容,删除文件,重命名文件等.同步到设置内的tools,请帮忙实现让AI省tokens又能准确的对文件增删改查的方案.
- 请给设置内的工具列表按重要性和相关性排个序
- 帮我给设置里的工具列表顶部总开关改一个样式,现在和列表条目一样,有点不好区分
- 让输入框可以粘贴图片和文件
- 移动端权限申请完善
- 增加通知工具：只在程序运行时有效，移动端本地推送通知，桌面端右下角弹框通知,
- 增加计时工具：比如让AI提醒我5分钟后喝水，系统计时5分钟后把消息回传给AI，让AI调用通知工具通知用户，AI可以对计时任务进行增删改查,
- 有计时任务的时候，移动端在通知栏常驻一个前台通知，尽可能保证app合法存活
- 用户的消息气泡增加复制功能
- 连续的工具消息折叠一下，折叠后只展示调用工具数量，展开后能查看每个工具的调用情况。
- 支持MCP功能,设置页面在技能右侧新增一个tab:MCP, 右下角和技能页差不多的新增按钮,弹出新增页面:可输入MCP名称, 然后复制粘贴mcp json可以加入一个对应的条目,AI可以通过mcp实现对应能力
- 内置2个角色: 猫娘和小秘
- 内置搜索mcp: bing搜索，bilibili，12306
- 在项目的设置-工具里面新增一个工具:Google Sheet,简介是：通过谷歌表格API让模型具有操作Google Sheet 的能力，
- 开关默认关闭，点击条目进入谷歌表格工具设置：一个文本输入框，输入谷歌表格的url或者 Spreadsheet ID,下面有一个测试连接按钮， 点击测试连接按钮，发起浏览器授权，
- 再下面是一个下拉选择按钮，用于选择模型默认操作的表名。右侧有一个刷新表格按钮，获取表格最新的表名列表。
- 用户点击开启开关，如果没有设置则跳转谷歌表格工具设置，且无法开启开关，只有保存设置后才能开启开关。
- 工具对谷歌表格操作的支持：操作范围：指定行，指定列，批量指定格子；操作内容：文字，文字属性，格子，格子属性；操作方法：增删改查，批量插入，批量修改，批量删除
- 优化工具的提示词：减少token占用，并能准确让模型知道怎么使用，无差错。
- 谷歌授权的密钥文件已放入 assets/json/client_secret.json
- 工具列表内的文字很多没有国际化处理，完善遗漏的国际化文字处理
- 展示图片的气泡优化，图片不要变形，采用填充裁切模式
- 本地模型推荐几个给用户下载


#### 未完成
- 桌面端增加一个给AI设置工作区的功能,让AI默认处理工作区内的文件.
- 新增频道，对接第三方聊天软件
- 模型加思考开关


#### 其它
搜索 skill :
```json5
{
  "url": "https://www.bing.com/search?q=关键词" 
}
```
内置本地模型下载过程中返回按钮没有效果，且下载完成后下载按钮还是显示，需要支持后台下载，需要支持断点续传，
如果文件丢失需要可以重新下载。