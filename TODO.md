# agent_buddy

Agent Buddy

目标:跨端智能体基座

### UI布局:
1.无论哪端,都采用手机样式.
2.APP启动后进入聊天页面.左上角设置按钮,点击进入设置页面.
3.设置页面,顶部tab:Provider,Role,Tools,Skill
Provider:模型提供商列表,可以新增,编辑,删除,启用.功能见下面功能详情.
Role:AI角色扮演列表,展示角色列表,可新增,编辑,删除,启用.
Tools:AI智能体工具列表,管理内置工具,先实现Fetch Web,可以让AI获取网址内容.
Skill:技能列表,与现有市面上技能兼容.
~~ UI样式采用IOS设计样式,简约素雅风格 ~~

### 功能:
#### 模型提供商:
1.先实现设置AI模型提供商页面,需要支持OpenAI和Anthropic协议.
2.可以添加多个提供商,添加页面输入名称, url, API key,需要根据协议自动补全path,有获取模型列表按钮.有测试连接按钮.

#### 聊天首页:
1.实现对话聊天页面,支持流式输出,支持不同模型的思考模式,AI聊天气泡5行思考流式展示,回复内容后,思考部分可展开至10行,上下滚动.
2.底部输入对话框最多展示10行,超过10行后可滚动.右边发送按钮与输入框底部对齐,整个输入框部分采用悬浮条设计.

#### 设置页面:
见上面UI布局.


### 代办事项
#### 已完成


#### 未完成
- 新增频道，对接第三方聊天软件（低优先级）
- 在模型设置页面新增支持文件类型多选项：文本，图片，音频，视频，文档。根据模型支持的文件类型决定发送给模型的文件是路径还是file。
- 聊天框输入@符号加入引用文件功能,用户输入@符号输入框上方立即弹出工作目录文件列表,继续输入文字模糊匹配列表内文件,按匹配程度排序。
- 用户点击列表中的文件则将文件展示在输入框上方的引用列表里,并把文件名展示在输入框,直接回车表示引用第一个匹配的文件。

- 怎么处理 word,excel,pdf 文件,
- 怎么解包zip文件,相关的类zip文件,jar,apk等


我想给项目实现离线Qwen3-TTS文字转语音系统,我想要把现有的气泡播放功能改成更自然的声音:
新增一套全平台纯离线Qwen3-TTS端侧TTS系统，四端支持Android/iOS/macOS/Windows。web平台采用原来的stts.
技术规范：
1. 底层：Rust + candle 运行量化Qwen3-TTS，FRB实现Dart与Rust FFI通信；
2. 禁止云端、禁止Python，全程本地离线推理；
3. 模型采用4bit量化，动态下载至APP缓存，不打包进安装包；
4. Flutter使用audioplayers播放推理输出的wav二进制流；
5. 提供统一工具类：初始化模型、文本合成、播放、暂停、停止、释放资源、本地音频缓存；
6. 适配各平台编译脚本：Android NDK交叉编译、iOS静态库编译、桌面端动态库编译；
7. 处理UI卡顿问题：Rust推理异步隔离子线程，回调通知UI刷新状态；
8. 增加完整异常处理、文件MD5校验、断点续下模型功能(可以复用现有的本地模型下载)。
9. 在设置页面新增TTS tab,放入关于Qwen3-TTS相关下载以及设置功能


#### 其它
搜索 skill :
```json5
{
  "url": "https://www.bing.com/search?q=关键词" 
}
```
