# agent_buddy

Agent Buddy

目标:跨端智能体基座

### 代办事项

#### 已完成
- 询问用户工具，选项被折叠在工具气泡内，用户无法知道被模型询问问题，请改为选项显示在模型回复气泡的底部。
- test 文件太多了，删掉无关紧要的测试用例
- 输入框@引用文件功能，显示的文件列表，需要包含所有子目录所有文件，需要能用上下方向键在列表内选择文件
- 项目修改模型配置会清空当前聊天问题，改为不清空聊天
- 我想给项目增加一个编辑图片的工具，让多模态模型可以编辑图片，这个工具只有支持图片的模型可以调用：
- 先实现:图片文件体积压缩，图片裁剪，图片分辨率调整（可选保持宽高比），图片旋转，等基础功能，
- 模型处理图片不要影响原图片文件，在项目的临时文件目录处理图片副本即可
- 每一步处理后发回给模型查看，然后在气泡内展示处理的图片给用户看，模型展示图片给用户看的气泡右下角需要有下载按钮，点击弹出选择保存地址弹框，供用户下载保存。
- 先讨论下实现方案 ，并看看有没有图片编辑的高级方案：可以编辑图片内容的第三方库等，
- 引用的图片没有给模型文件路径，导致模型编辑图片不知道路径，请优化
- 加一个图片格式转换功能，支持常见图片格式转换，比如把png转成webp
- 完善工具遗漏的_toolUserDescription面向用户描述
- 项目工具比较多，每次对话消耗的token很多，请给出方案让工具动态加载，先让模型知道有这个工具，需要时再加载工具使用详情？
- 我要在不影响模型使用工具情况下，极限压缩基础token数量。
- 有些模型提供商按次数计费，优化工具load_tool实现批量加载工具详情功能，减少模型请求次数的同时也能优化token消耗
- 图片工具：增加裁剪圆形图片和圆角图片功能，图片左右翻转/上下翻转功能，
- 模型回复气泡优化：长任务下思考/工具/正文都堆在一个气泡内，工具调用次数容易突破上限、正文气泡过长。改为每个 orchestrator 轮次一个气泡，中间轮气泡只显示时间+复制按钮+时长，最终轮保留完整 metrics。
- 文件编辑工具优化，替换字符串匹配逻辑成功率太低：有可能因为换行符，空格数，字符编码等问题，导致匹配失败，且编辑失败工具返回的结果却是成功。
- 建议优化：编辑文件用行号匹配，参数：开始行号，结束行号，文本内容; 只需要3个参数即可，结束行号不能小于开始行号，逻辑就是把指定行号内容替换成参数的文本内容。
- 插入新内容：内容需含有原行号的文本，删除文本为空即可，开始行号和结束行号可以相同，可以省略结束行号，省略结束行号意味着只编辑开始行号这一行，编辑的位置同时包含开始行号和结束行号
- 批量编辑时，先编辑行号大的，再编辑行号小的，防止编辑小的行号影响后面行号变化。一个文件编辑完后记得刷新文件缓存。
- 优化项目命令行工具在windows系统内的表现。cmd 对智能体不是很友好，
- 检查系统内是否有 git bash,如果有优先用 git bash 做命令行工具，其次用 PowerShell. 其次再用cmd 兜底。
- 询问用户问题工具增加可以同时询问多个问题功能：回答完一个展示下一个，顶部展示问题总数，用户回答问题除了AI给出的选项，还可以手动输入答案。
- Windows 环境中有 Wsl的bash,也有 git的bash,项目的命令行工具识别的bash工具错误,识别到了wsl的bash工具,但是不可用,请通过git的路径找到对应的bash工具位置.


#### BUG修复
- mac 上无法选择工作目录问题， 点击工作目录没有反应。
- mac Google Sheet 工具无法授权问题，无法启动本地服务授权


#### 桌宠开发：
开源桌宠素材下载网站：https://petdex.dev/
下载的素材是  Spritesheet（精灵图）：一张png有角色各种动作。
在 Flutter 中，你可以使用 sprite_sheet 或 flutter_animate 等相关插件 ：flutter_spritesheet_animation: ^1.0.3
只需提供这张大图，并配置好每张小图的宽高（frameWidth/frameHeight）、列数（columns）以及动作对应的帧范围（startFrame - endFrame），
Flutter 就能自动帮你裁剪并播放出流畅的动画了
https://petdex.dev/pets/anya-2 阿尼亚


#### 未完成
- 后续功能开发方向，桌面的加一个桌宠。做一个真正的AI桌宠。
- 常规设置页加入桌面端开机启动功能

- 怎么处理 word,excel,pdf 文件,
- 怎么解包zip文件,相关的类zip文件,jar,apk等

- 图片bbox检测，返回图片物体bbox:（坐标不用像素，用图片宽高百分比位置，这样图片缩放也能保证位置的准确性）
例子：
```json5
{
  "objects": [
    {
      "label": "猫",
      "confidence": 0.96,
      "bbox": [0.11, 0.18, 0.95, 0.88] // x1,y1左上角 x2,y2右下角图片
    }
  ]
}
```


#### 其它
搜索 skill :
```json5
{
  "url": "https://www.bing.com/search?q=关键词" 
}
```
